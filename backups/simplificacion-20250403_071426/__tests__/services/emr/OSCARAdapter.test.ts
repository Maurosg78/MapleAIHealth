import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('OSCARAdapter.test', () => {
  it('should pass basic test', () => {
    expect(true).toBe(true);
  });
});
